class star {

    constructor(x, y, mill) {
        this.ini = mill
        this.x = x 
        this.y = y
    }

    dibujar(mill) {
        if (mill - ini < 200) {
            stage1()
        }
    }

    stage1() {
        rect(x - 4, y - 4, )
    }

}